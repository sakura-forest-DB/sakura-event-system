<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body { background-color: #f7fdf8; }
    .navbar-brand { font-weight: bold; color: #2d5a27 !important; }
    .btn-success { background-color: #2d5a27; border-color: #2d5a27; }
    .btn-success:hover { background-color: #1e3c1a; border-color: #1e3c1a; }
    .form-section { border-left: 4px solid #2d5a27; padding-left: 15px; margin-bottom: 2rem; }
    footer { background-color: #f8f9fa; margin-top: auto; }
  </style>
</head>
<body class="d-flex flex-column min-vh-100">
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
      <a class="navbar-brand" href="/">🌸 菊名桜山公園</a>
    </div>
  </nav>

  <main class="container my-4 flex-grow-1">
    <div class="row justify-content-center">
      <div class="col-md-10">
        <div class="card shadow-sm">
          <div class="card-header bg-success text-white">
            <h4 class="mb-0">🏪 出店申込内容確認</h4>
            <small class="text-light">内容をご確認の上、「✅ 内容確認後、送信する」を押してください</small>
          </div>

          <div class="card-body">

            <% 
              // ====== 値のヘルパ ======
              const D = formData || {};
              const pick = (...keys) => {
                for (const k of keys) { if (D && D[k] != null && String(D[k]).trim() !== '') return String(D[k]); }
                return '';
              };
              const show = (v, fallback='未入力') => (v && String(v).trim() !== '' ? v : fallback);
              const yn   = (v) => (v ? '同意済み' : '—');

              // 代表的な別名にも対応（旧/新フィールド両対応）
              const groupName   = pick('groupName','orgName','name');
              const representative = pick('representative','contactName');
              const email       = pick('email');
              const phone       = pick('phone','tel');
              const address     = pick('address');

              // 出店詳細
              const boothType   = pick('boothType','stallType');          // 飲食/物販 など
              const boothCount  = pick('boothCount','plotCount','blockCount');
              const stallContent= pick('stallContent','content','description');
              const salesItems  = pick('salesItems','items');
              const priceMin    = pick('priceRangeMin','priceMin');
              const priceMax    = pick('priceRangeMax','priceMax');

              // レンタル類
              const rentalTables = pick('rentalTables','tables');
              const rentalChairs = pick('rentalChairs','chairs');

              // その他
              const questions   = pick('questions','requests','notes');

              // 同意
              const privacyConsent   = !!(D.privacyConsent || D.agreePrivacy);
              const marketingConsent = !!(D.marketingConsent || D.agreePublicity);
            %>

            <!-- 基本情報 -->
            <div class="form-section">
              <h5>📝 基本情報</h5>
              <dl class="row mb-0">
                <dt class="col-4 col-sm-3">参加団体名</dt>
                <dd class="col-8 col-sm-9"><%= show(groupName) %></dd>

                <dt class="col-4 col-sm-3">代表者名</dt>
                <dd class="col-8 col-sm-9"><%= show(representative) %></dd>

                <dt class="col-4 col-sm-3">メールアドレス</dt>
                <dd class="col-8 col-sm-9"><%= show(email) %></dd>

                <dt class="col-4 col-sm-3">電話番号</dt>
                <dd class="col-8 col-sm-9"><%= show(phone) %></dd>

                <dt class="col-4 col-sm-3">住所</dt>
                <dd class="col-8 col-sm-9"><%= show(address,'—') %></dd>
              </dl>
            </div>

            <!-- 出店内容 -->
            <div class="form-section">
              <h5>🏷️ 出店内容</h5>
              <dl class="row mb-0">
                <dt class="col-4 col-sm-3">出店種別</dt>
                <dd class="col-8 col-sm-9"><%= show(boothType,'—') %></dd>

                <dt class="col-4 col-sm-3">希望区画</dt>
                <dd class="col-8 col-sm-9"><%= show(boothCount,'—') %> 枠</dd>

                <dt class="col-4 col-sm-3">出店内容</dt>
                <dd class="col-8 col-sm-9"><%= show(stallContent) %></dd>

                <dt class="col-4 col-sm-3">販売品目</dt>
                <dd class="col-8 col-sm-9"><%= show(salesItems) %></dd>

                <dt class="col-4 col-sm-3">価格帯</dt>
                <dd class="col-8 col-sm-9">
                  <% if (priceMin || priceMax) { %>
                    <%= priceMin ? (priceMin + '円') : '—' %> 〜 <%= priceMax ? (priceMax + '円') : '—' %>
                  <% } else { %>
                    — 
                  <% } %>
                </dd>

                <dt class="col-4 col-sm-3">レンタル：机</dt>
                <dd class="col-8 col-sm-9"><%= show(rentalTables,'0') %> 台</dd>

                <dt class="col-4 col-sm-3">レンタル：椅子</dt>
                <dd class="col-8 col-sm-9"><%= show(rentalChairs,'0') %> 脚</dd>

                <dt class="col-4 col-sm-3">連絡事項</dt>
                <dd class="col-8 col-sm-9"><%= show(questions,'—') %></dd>
              </dl>
            </div>

            <!-- 同意事項 -->
            <div class="form-section">
              <h5>🔒 同意事項</h5>
              <dl class="row mb-0">
                <dt class="col-6 col-sm-4">個人情報の取扱いに同意</dt>
                <dd class="col-6 col-sm-8"><%= yn(privacyConsent) %></dd>

                <dt class="col-6 col-sm-4">広報等での使用に同意</dt>
                <dd class="col-6 col-sm-8"><%= yn(marketingConsent) %></dd>
              </dl>
            </div>

            <!-- 確認・送信ボタン -->
            <div class="d-grid gap-2 d-md-flex justify-content-md-end mt-4">
              <!-- 戻って修正：GETで入力画面へ（セッション草稿を復元して再表示） -->
              <form method="GET" action="/apply/<%= event.slug %>/stall" class="me-md-2">
                <button type="submit" class="btn btn-secondary">✏️ 戻って修正</button>
              </form>

              <!-- 送信：POSTで /submit -->
              <form method="POST" action="/apply/<%= event.slug %>/stall/submit">
                <button type="submit" class="btn btn-success">✅ 内容確認後、送信する</button>
              </form>
            </div>

          </div>
        </div>
      </div>
    </div>
  </main>

  <footer class="py-3 mt-4">
    <div class="container text-center text-muted">
      <p>&copy; 2025 菊名桜山公園 ボランティア管理システム</p>
    </div>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>