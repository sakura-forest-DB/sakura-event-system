<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .navbar-brand { font-weight: bold; color: #2d5a27 !important; }
        .bg-primary { background-color: #2d5a27 !important; }
        .btn-primary { background-color: #2d5a27; border-color: #2d5a27; }
        .btn-primary:hover { background-color: #1e3c1a; border-color: #1e3c1a; }
        footer { background-color: #f8f9fa; margin-top: auto; }
    </style>
</head>
<body class="d-flex flex-column min-vh-100">
    <!-- 管理者モード表示 -->
    <div class="alert alert-warning mb-0 text-center border-0 rounded-0" style="background: linear-gradient(45deg, #ffc107, #ffca2c);">
        <strong>🔧 管理者モード</strong> - 管理者として操作中です
    </div>
    
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container">
            <a class="navbar-brand" href="/">🌸 菊名桜山公園</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/">ホーム</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/register">ボランティア登録</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/apply">イベント申込</a>
                    </li>
                </ul>
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="/admin">管理画面</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <main class="container my-4 flex-grow-1">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h2>管理画面</h2>
            <div>
                <a href="/admin/performers/export" class="btn btn-success">CSVエクスポート</a>
                <a href="/admin/logout" class="btn btn-outline-secondary">ログアウト</a>
            </div>
        </div>

        <!-- 管理者タブメニュー -->
        <ul class="nav nav-tabs mb-4">
            <li class="nav-item">
                <a class="nav-link" href="/admin/volunteers">ボランティア</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/admin/stalls">出店申し込み</a>
            </li>
            <li class="nav-item">
                <a class="nav-link active" href="/admin/performers">出演申し込み</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/admin/events">イベント管理</a>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="previewDropdown" role="button" data-bs-toggle="dropdown">プレビュー</a>
                <ul class="dropdown-menu">
                    <li><a class="dropdown-item" href="/admin/preview/volunteer-complete" target="_blank">🌱 ボランティア登録完了画面</a></li>
                    <li><hr class="dropdown-divider"></li>
                    <li><a class="dropdown-item" href="/admin/preview/application-confirm?type=stall" target="_blank">🏪 出店申込確認画面</a></li>
                    <li><a class="dropdown-item" href="/admin/preview/application-complete?type=stall" target="_blank">🏪 出店申込完了画面</a></li>
                    <li><hr class="dropdown-divider"></li>
                    <li><a class="dropdown-item" href="/admin/preview/application-confirm?type=performer" target="_blank">🎭 出演申込確認画面</a></li>
                    <li><a class="dropdown-item" href="/admin/preview/application-complete?type=performer" target="_blank">🎭 出演申込完了画面</a></li>
                </ul>
            </li>
        </ul>

        <!-- 検索・絞り込みフォーム -->
        <div class="card mb-4">
            <div class="card-body">
                <form method="GET" action="/admin/performers" class="row g-3">
                    <div class="col-md-6">
                        <label class="form-label">キーワード検索</label>
                        <input type="text" class="form-control" name="search" value="<%= filters.search || '' %>" 
                               placeholder="団体名、代表者名、メール">
                    </div>
                    <div class="col-md-3">
                        <label class="form-label">出演内容</label>
                        <input type="text" class="form-control" name="performance" value="<%= filters.performance || '' %>" 
                               placeholder="音楽、ダンス等">
                    </div>
                    <div class="col-md-3 d-flex align-items-end">
                        <button type="submit" class="btn btn-primary me-2">検索</button>
                        <a href="/admin/performers" class="btn btn-outline-secondary">クリア</a>
                    </div>
                </form>
            </div>
        </div>

        <!-- 出演申込一覧 -->
        <div class="card">
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table table-striped">
                        <thead>
                            <tr>
                                <th>団体名</th>
                                <th>代表者</th>
                                <th>出演内容</th>
                                <th>イベント</th>
                                <th>メール・電話</th>
                                <th>詳細情報</th>
                                <th>申込日</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% if (performerApplications.length > 0) { %>
                                <% performerApplications.forEach(performer => { %>
                                    <tr>
                                        <td>
                                            <strong><a href="/admin/performers/<%= performer.id %>" class="text-decoration-none"><%= performer.groupName %></a></strong>
                                            <% if (performer.changeLogs && performer.changeLogs.length > 0) { %>
                                                <span class="badge bg-warning ms-1" title="編集済み（<%= performer.changeLogs.length %>回変更）">✏️</span>
                                            <% } %>
                                            <% if (performer.adminNotes && performer.adminNotes.length > 0) { %>
                                                <span class="badge bg-success ms-1" title="管理メモ <%= performer.adminNotes.length %>件">📝 <%= performer.adminNotes.length %></span>
                                            <% } %>
                                        </td>
                                        <td><%= performer.representative %></td>
                                        <td>
                                            <div class="small">
                                                <%= performer.performance.length > 40 ? performer.performance.substring(0, 40) + '...' : performer.performance %>
                                            </div>
                                            <% if (performer.performerCount) { %>
                                                <small class="text-muted"><%= performer.performerCount %>人</small>
                                            <% } %>
                                            <% if (performer.slotCount) { %>
                                                <small class="text-muted"> / <%= performer.slotCount %>枠希望</small>
                                            <% } %>
                                        </td>
                                        <td>
                                            <% if (performer.event) { %>
                                                <strong><%= performer.event.title %></strong>
                                                <small class="d-block text-muted"><%= new Date(performer.event.date).toLocaleDateString('ja-JP') %></small>
                                            <% } else { %>
                                                <span class="text-muted">イベント未選択</span>
                                            <% } %>
                                        </td>
                                        <td>
                                            <div class="small">
                                                <div><%= performer.email %></div>
                                                <% if (performer.phone) { %>
                                                    <div class="text-muted"><%= performer.phone %></div>
                                                <% } %>
                                            </div>
                                        </td>
                                        <td>
                                            <div class="small">
                                                <% if (performer.vehicleCount) { %>
                                                    <div><strong>車両:</strong> <%= performer.vehicleCount %>台</div>
                                                <% } %>
                                                <% if (performer.rentalAmp) { %>
                                                    <div><strong>拡声装置:</strong> <%= performer.rentalAmp %>台</div>
                                                <% } %>
                                                <% if (performer.rentalMic) { %>
                                                    <div><strong>追加マイク:</strong> <%= performer.rentalMic %>本</div>
                                                <% } %>
                                                <% if (performer.questions && performer.questions.trim()) { %>
                                                    <div><strong>要望:</strong> <%= performer.questions.length > 20 ? performer.questions.substring(0, 20) + '...' : performer.questions %></div>
                                                <% } %>
                                            </div>
                                        </td>
                                        <td><%= new Date(performer.createdAt).toLocaleDateString('ja-JP') %></td>
                                    </tr>
                                <% }); %>
                            <% } else { %>
                                <tr>
                                    <td colspan="7" class="text-center text-muted">該当する出演申込が見つかりません</td>
                                </tr>
                            <% } %>
                        </tbody>
                    </table>
                </div>

                <!-- ページネーション -->
                <% if (pagination.total > 1) { %>
                    <nav aria-label="ページネーション">
                        <ul class="pagination justify-content-center">
                            <% if (pagination.hasPrev) { %>
                                <li class="page-item">
                                    <a class="page-link" href="?page=<%= pagination.current - 1 %>&<%= new URLSearchParams(filters).toString() %>">前へ</a>
                                </li>
                            <% } %>
                            
                            <% for (let i = Math.max(1, pagination.current - 2); i <= Math.min(pagination.total, pagination.current + 2); i++) { %>
                                <li class="page-item <%= i === pagination.current ? 'active' : '' %>">
                                    <a class="page-link" href="?page=<%= i %>&<%= new URLSearchParams(filters).toString() %>"><%= i %></a>
                                </li>
                            <% } %>
                            
                            <% if (pagination.hasNext) { %>
                                <li class="page-item">
                                    <a class="page-link" href="?page=<%= pagination.current + 1 %>&<%= new URLSearchParams(filters).toString() %>">次へ</a>
                                </li>
                            <% } %>
                        </ul>
                    </nav>
                <% } %>
            </div>
        </div>
    </main>

    <footer class="py-3 mt-4">
        <div class="container text-center text-muted">
            <p>&copy; 2025 菊名桜山公園 ボランティア管理システム</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>