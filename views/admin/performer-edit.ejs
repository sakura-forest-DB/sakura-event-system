<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .navbar-brand { font-weight: bold; color: #2d5a27 !important; }
        .bg-primary { background-color: #2d5a27 !important; }
        .btn-primary { background-color: #2d5a27; border-color: #2d5a27; }
        .btn-primary:hover { background-color: #1e3c1a; border-color: #1e3c1a; }
        footer { background-color: #f8f9fa; margin-top: auto; }
        .form-section { border-left: 4px solid #2d5a27; padding-left: 15px; margin-bottom: 2rem; }
        .required { color: #dc3545; }
    </style>
</head>
<body class="d-flex flex-column min-vh-100">
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container">
            <a class="navbar-brand" href="/">🌸 菊名桜山公園</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/">ホーム</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/register">ボランティア登録</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/apply">イベント申込</a>
                    </li>
                </ul>
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="/admin">管理画面</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <main class="container my-4 flex-grow-1">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h2>出演申込編集</h2>
            <div>
                <a href="/admin/performers/<%= performer.id %>" class="btn btn-outline-secondary">詳細に戻る</a>
            </div>
        </div>

        <% if (errors && errors.length > 0) { %>
            <div class="alert alert-danger">
                <ul class="mb-0">
                    <% errors.forEach(error => { %>
                        <li><%= error %></li>
                    <% }); %>
                </ul>
            </div>
        <% } %>

        <form method="POST" action="/admin/performers/<%= performer.id %>/update">
            <div class="card">
                <div class="card-header">
                    <h4 class="mb-0">🎭 出演申込情報</h4>
                </div>
                <div class="card-body">
                    <!-- 基本情報 -->
                    <div class="form-section">
                        <h5>📝 基本情報</h5>
                        <div class="row mb-3">
                            <div class="col-md-6">
                                <label for="groupName" class="form-label">参加団体名 <span class="required">*</span></label>
                                <input type="text" class="form-control" id="groupName" name="groupName" 
                                       value="<%= performer.groupName %>" required>
                            </div>
                            <div class="col-md-6">
                                <label for="representative" class="form-label">代表者名 <span class="required">*</span></label>
                                <input type="text" class="form-control" id="representative" name="representative" 
                                       value="<%= performer.representative %>" required>
                            </div>
                        </div>

                        <div class="row mb-3">
                            <div class="col-md-6">
                                <label for="email" class="form-label">メールアドレス <span class="required">*</span></label>
                                <input type="email" class="form-control" id="email" name="email" 
                                       value="<%= performer.email %>" required>
                            </div>
                            <div class="col-md-6">
                                <label for="phone" class="form-label">電話番号 <span class="required">*</span></label>
                                <input type="tel" class="form-control" id="phone" name="phone" 
                                       value="<%= performer.phone %>" required>
                            </div>
                        </div>

                        <div class="mb-3">
                            <label for="address" class="form-label">住所</label>
                            <input type="text" class="form-control" id="address" name="address" 
                                   value="<%= performer.address || '' %>">
                        </div>
                    </div>

                    <!-- 出演内容 -->
                    <div class="form-section">
                        <h5>🎭 出演内容</h5>
                        <div class="mb-3">
                            <label for="performance" class="form-label">出演内容 <span class="required">*</span></label>
                            <textarea class="form-control" id="performance" name="performance" rows="4" required><%= performer.performance %></textarea>
                        </div>

                        <div class="row mb-3">
                            <div class="col-md-6">
                                <label for="performerCount" class="form-label">出演者数</label>
                                <input type="number" class="form-control" id="performerCount" name="performerCount" min="1"
                                       value="<%= performer.performerCount || '' %>">
                            </div>
                            <div class="col-md-6">
                                <label for="slotCount" class="form-label">希望出演枠数</label>
                                <input type="number" class="form-control" id="slotCount" name="slotCount" min="1"
                                       value="<%= performer.slotCount || '' %>">
                            </div>
                        </div>
                    </div>

                    <!-- 音響・機材 -->
                    <div class="form-section">
                        <h5>🔊 レンタル拡声装置</h5>
                        
                        <div class="mb-3">
                            <label for="audioSourceOnly" class="form-label">音源再生利用</label>
                            <input type="number" class="form-control" id="audioSourceOnly" name="audioSourceOnly" min="0"
                                   value="<%= performer.audioSourceOnly || '' %>">
                            <div class="form-text">枠分（音源再生装置はご持参ください）</div>
                        </div>
                        
                        <div class="row mb-3">
                            <div class="col-md-6">
                                <label for="rentalAmp" class="form-label">音源拡声装置＋マイク１本セット</label>
                                <input type="number" class="form-control" id="rentalAmp" name="rentalAmp" min="0"
                                       value="<%= performer.rentalAmp || '' %>">
                                <div class="form-text">５００円 /１枠</div>
                            </div>
                            <div class="col-md-6">
                                <label for="rentalMic" class="form-label">追加マイク本数</label>
                                <input type="number" class="form-control" id="rentalMic" name="rentalMic" min="0"
                                       value="<%= performer.rentalMic || '' %>">
                                <div class="form-text">追加マイク１本：１００円 /１枠（レンタル可能数：２本）</div>
                            </div>
                        </div>
                    </div>

                    <!-- 搬入搬出 -->
                    <div class="form-section">
                        <h5>🚚 搬入搬出</h5>
                        <div class="row mb-3">
                            <div class="col-md-6">
                                <label for="vehicleCount" class="form-label">車両台数</label>
                                <input type="number" class="form-control" id="vehicleCount" name="vehicleCount" min="0"
                                       value="<%= performer.vehicleCount || '' %>">
                                <div class="form-text">（２台以上の場合は要相談）</div>
                            </div>
                            <div class="col-md-6">
                                <label for="vehicleNumbers" class="form-label">車両ナンバー</label>
                                <input type="text" class="form-control" id="vehicleNumbers" name="vehicleNumbers"
                                       value="<%= performer.vehicleNumbers || '' %>" 
                                       placeholder="例: 横浜123あ4567（複数台の場合はカンマ区切り）">
                            </div>
                        </div>
                        
                        <div class="alert alert-warning">
                            <strong>⚠️ 注意事項:</strong> 開催時間内は車両の移動はできません
                        </div>
                    </div>

                    <!-- 連絡事項 -->
                    <div class="form-section">
                        <h5>💬 連絡事項</h5>
                        <div class="mb-3">
                            <label for="questions" class="form-label">ご質問・ご要望等</label>
                            <textarea class="form-control" id="questions" name="questions" rows="4"><%= performer.questions || '' %></textarea>
                        </div>
                    </div>

                    <!-- 個人情報同意確認 -->
                    <div class="form-section">
                        <h5>🔒 個人情報の取り扱い同意</h5>
                        <div class="mb-3">
                            <div class="alert alert-light">
                                <div class="form-check mb-2">
                                    <span class="form-check-input" style="background-color: #d4edda; border-color: #28a745;">✓</span>
                                    <label class="form-check-label ms-2">
                                        ご入力いただいた個人情報を、本イベントの運営に必要な範囲で利用することに同意します。（必須）
                                    </label>
                                </div>
                                <div class="form-check">
                                    <span class="form-check-input" style="background-color: <%= performer.marketingConsent ? '#d4edda' : '#f8f9fa' %>; border-color: <%= performer.marketingConsent ? '#28a745' : '#6c757d' %>;">
                                        <%= performer.marketingConsent ? '✓' : '✗' %>
                                    </span>
                                    <label class="form-check-label ms-2">
                                        ご入力いただいた個人情報を、今後のイベントのお知らせに利用することに<%= performer.marketingConsent ? '同意します' : '同意しません' %>。（任意）
                                    </label>
                                </div>
                                <p class="small text-muted mt-2 mb-0">※個人情報同意は申込者本人による変更のみ可能です</p>
                            </div>
                        </div>
                    </div>

                    <!-- 変更理由・編集者 -->
                    <div class="form-section">
                        <h5>📋 変更情報</h5>
                        <div class="row mb-3">
                            <div class="col-md-6">
                                <label for="editor" class="form-label">編集者名 <span class="required">*</span></label>
                                <input type="text" class="form-control" id="editor" name="editor" required
                                       placeholder="例: 田中太郎">
                            </div>
                            <div class="col-md-6">
                                <label for="reason" class="form-label">変更理由 <span class="required">*</span></label>
                                <input type="text" class="form-control" id="reason" name="reason" required
                                       placeholder="例: 申込者からの変更依頼">
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="text-center mt-4">
                <button type="submit" class="btn btn-primary btn-lg me-3">💾 保存</button>
                <a href="/admin/performers/<%= performer.id %>" class="btn btn-outline-secondary btn-lg">キャンセル</a>
            </div>
        </form>
    </main>

    <footer class="py-3 mt-4">
        <div class="container text-center text-muted">
            <p>&copy; 2025 菊名桜山公園 ボランティア管理システム</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>