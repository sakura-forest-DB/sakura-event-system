<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .navbar-brand { font-weight: bold; color: #2d5a27 !important; }
        .bg-primary { background-color: #2d5a27 !important; }
        .btn-primary { background-color: #2d5a27; border-color: #2d5a27; }
        .btn-primary:hover { background-color: #1e3c1a; border-color: #1e3c1a; }
        footer { background-color: #f8f9fa; margin-top: auto; }
    </style>
</head>
<body class="d-flex flex-column min-vh-100">
    <!-- 管理者モード表示 -->
    <div class="alert alert-warning mb-0 text-center border-0 rounded-0" style="background: linear-gradient(45deg, #ffc107, #ffca2c);">
        <strong>🔧 管理者モード</strong> - 管理者として操作中です
    </div>
    
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container">
            <a class="navbar-brand" href="/">🌸 菊名桜山公園</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/">ホーム</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/register">ボランティア登録</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/apply">イベント申込</a>
                    </li>
                </ul>
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="/admin">管理画面</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <main class="container my-4 flex-grow-1">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h2>管理画面</h2>
            <div>
                <a href="/admin/volunteers/export" class="btn btn-success">CSVエクスポート</a>
                <a href="/admin/logout" class="btn btn-outline-secondary">ログアウト</a>
            </div>
        </div>

        <!-- 管理者タブメニュー -->
        <ul class="nav nav-tabs mb-4">
            <li class="nav-item">
                <a class="nav-link active" href="/admin/volunteers">ボランティア</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/admin/stalls">出店申し込み</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/admin/performers">出演申し込み</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/admin/events">イベント管理</a>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="previewDropdown" role="button" data-bs-toggle="dropdown">プレビュー</a>
                <ul class="dropdown-menu">
                    <li><a class="dropdown-item" href="/admin/preview/volunteer-complete" target="_blank">🌱 ボランティア登録完了画面</a></li>
                    <li><hr class="dropdown-divider"></li>
                    <li><a class="dropdown-item" href="/admin/preview/application-confirm?type=stall" target="_blank">🏪 出店申込確認画面</a></li>
                    <li><a class="dropdown-item" href="/admin/preview/application-complete?type=stall" target="_blank">🏪 出店申込完了画面</a></li>
                    <li><hr class="dropdown-divider"></li>
                    <li><a class="dropdown-item" href="/admin/preview/application-confirm?type=performer" target="_blank">🎭 出演申込確認画面</a></li>
                    <li><a class="dropdown-item" href="/admin/preview/application-complete?type=performer" target="_blank">🎭 出演申込完了画面</a></li>
                </ul>
            </li>
        </ul>

        <!-- 検索・絞り込みフォーム -->
        <div class="card mb-4">
            <div class="card-body">
                <form method="GET" action="/admin/volunteers" class="row g-3">
                    <div class="col-md-3">
                        <label class="form-label">キーワード検索</label>
                        <input type="text" class="form-control" name="search" value="<%= filters.search || '' %>" 
                               placeholder="氏名、メール、電話番号">
                    </div>
                    <div class="col-md-2">
                        <label class="form-label">イベント</label>
                        <select class="form-control" name="event">
                            <option value="">全て</option>
                            <% events.forEach(event => { %>
                                <option value="<%= event.id %>" <%= filters.event === event.id ? 'selected' : '' %>>
                                    <%= event.title %>
                                </option>
                            <% }); %>
                        </select>
                    </div>
                    <div class="col-md-2">
                        <label class="form-label">役割</label>
                        <select class="form-control" name="role">
                            <option value="">全て</option>
                            <option value="保全" <%= filters.role === '保全' ? 'selected' : '' %>>保全</option>
                            <option value="出演" <%= filters.role === '出演' ? 'selected' : '' %>>出演</option>
                            <option value="出店" <%= filters.role === '出店' ? 'selected' : '' %>>出店</option>
                            <option value="運営" <%= filters.role === '運営' ? 'selected' : '' %>>運営</option>
                        </select>
                    </div>
                    <div class="col-md-3 d-flex align-items-end">
                        <button type="submit" class="btn btn-primary me-2">検索</button>
                        <a href="/admin/volunteers" class="btn btn-outline-secondary">クリア</a>
                    </div>
                </form>
            </div>
        </div>

        <!-- ボランティア一覧 -->
        <div class="card">
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table table-striped">
                        <thead>
                            <tr>
                                <th>氏名</th>
                                <th>種別</th>
                                <th>メール</th>
                                <th>電話</th>
                                <th>スキル</th>
                                <th>申込状況</th>
                                <th>登録日</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% if (volunteers.length > 0) { %>
                                <% volunteers.forEach(volunteer => { %>
                                    <tr>
                                        <td>
                                            <a href="/admin/volunteers/<%= volunteer.id %>" class="text-decoration-none">
                                                <strong><%= volunteer.name %></strong>
                                                <% if (volunteer.changeLogs && volunteer.changeLogs.length > 0) { %>
                                                    <span class="badge bg-warning ms-1" title="最終更新: <%= volunteer.changeLogs[0].createdAt.toLocaleDateString('ja-JP') %>">編集済</span>
                                                <% } %>
                                            </a>
                                            <% if (volunteer.orgName) { %>
                                                <br><small class="text-muted">(<%= volunteer.orgName %>)</small>
                                            <% } %>
                                        </td>
                                        <td><span class="badge bg-secondary"><%= volunteer.type === 'individual' ? '個人' : '団体' %></span></td>
                                        <td><%= volunteer.email %></td>
                                        <td><%= volunteer.phone || '-' %></td>
                                        <td>
                                            <% if (volunteer.skills.length > 0) { %>
                                                <% volunteer.skills.forEach(skill => { %>
                                                    <span class="badge bg-info me-1"><%= skill %></span>
                                                <% }); %>
                                            <% } else { %>
                                                <span class="text-muted">-</span>
                                            <% } %>
                                        </td>
                                        <td>
                                            <% if (volunteer.signups.length > 0) { %>
                                                <% volunteer.signups.forEach(signup => { %>
                                                    <div class="small mb-1">
                                                        <strong><%= signup.event.title %></strong> - <%= signup.role %>
                                                        <span class="badge bg-<%= signup.status === 'applied' ? 'warning' : signup.status === 'accepted' ? 'success' : 'danger' %>">
                                                            <%= signup.status === 'applied' ? '申込中' : signup.status === 'accepted' ? '承認' : '辞退' %>
                                                        </span>
                                                    </div>
                                                <% }); %>
                                            <% } else { %>
                                                <span class="text-muted">申込なし</span>
                                            <% } %>
                                        </td>
                                        <td><%= new Date(volunteer.createdAt).toLocaleDateString('ja-JP') %></td>
                                    </tr>
                                <% }); %>
                            <% } else { %>
                                <tr>
                                    <td colspan="7" class="text-center text-muted">該当するボランティアが見つかりません</td>
                                </tr>
                            <% } %>
                        </tbody>
                    </table>
                </div>

                <!-- ページネーション -->
                <% if (pagination.total > 1) { %>
                    <nav aria-label="ページネーション">
                        <ul class="pagination justify-content-center">
                            <% if (pagination.hasPrev) { %>
                                <li class="page-item">
                                    <a class="page-link" href="?page=<%= pagination.current - 1 %>&<%= new URLSearchParams(filters).toString() %>">前へ</a>
                                </li>
                            <% } %>
                            
                            <% for (let i = Math.max(1, pagination.current - 2); i <= Math.min(pagination.total, pagination.current + 2); i++) { %>
                                <li class="page-item <%= i === pagination.current ? 'active' : '' %>">
                                    <a class="page-link" href="?page=<%= i %>&<%= new URLSearchParams(filters).toString() %>"><%= i %></a>
                                </li>
                            <% } %>
                            
                            <% if (pagination.hasNext) { %>
                                <li class="page-item">
                                    <a class="page-link" href="?page=<%= pagination.current + 1 %>&<%= new URLSearchParams(filters).toString() %>">次へ</a>
                                </li>
                            <% } %>
                        </ul>
                    </nav>
                <% } %>
            </div>
        </div>
    </main>

    <footer class="py-3 mt-4">
        <div class="container text-center text-muted">
            <p>&copy; 2025 菊名桜山公園 ボランティア管理システム</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>