<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .navbar-brand { font-weight: bold; color: #2d5a27 !important; }
        .bg-primary { background-color: #2d5a27 !important; }
        .btn-primary { background-color: #2d5a27; border-color: #2d5a27; }
        .btn-primary:hover { background-color: #1e3c1a; border-color: #1e3c1a; }
        footer { background-color: #f8f9fa; margin-top: auto; }
        .form-section { border-left: 4px solid #2d5a27; padding-left: 15px; margin-bottom: 2rem; }
        .required { color: #dc3545; }
    </style>
</head>
<body class="d-flex flex-column min-vh-100">
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container">
            <a class="navbar-brand" href="/">🌸 菊名桜山公園</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/">ホーム</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/register">ボランティア登録</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/apply">イベント申込</a>
                    </li>
                </ul>
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="/admin">管理画面</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <main class="container my-4 flex-grow-1">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h2>出店申込編集</h2>
            <div>
                <a href="/admin/stalls/<%= stall.id %>" class="btn btn-outline-secondary">詳細に戻る</a>
            </div>
        </div>

        <% if (errors && errors.length > 0) { %>
            <div class="alert alert-danger">
                <ul class="mb-0">
                    <% errors.forEach(error => { %>
                        <li><%= error %></li>
                    <% }); %>
                </ul>
            </div>
        <% } %>

        <form method="POST" action="/admin/stalls/<%= stall.id %>/update">
            <div class="card">
                <div class="card-header">
                    <h4 class="mb-0">🏪 出店申込情報</h4>
                </div>
                <div class="card-body">
                    <!-- 基本情報 -->
                    <div class="form-section">
                        <h5>📝 基本情報</h5>
                        <div class="row mb-3">
                            <div class="col-md-6">
                                <label for="groupName" class="form-label">参加団体名 <span class="required">*</span></label>
                                <input type="text" class="form-control" id="groupName" name="groupName" 
                                       value="<%= stall.groupName %>" required>
                            </div>
                            <div class="col-md-6">
                                <label for="representative" class="form-label">代表者名 <span class="required">*</span></label>
                                <input type="text" class="form-control" id="representative" name="representative" 
                                       value="<%= stall.representative %>" required>
                            </div>
                        </div>

                        <div class="row mb-3">
                            <div class="col-md-6">
                                <label for="email" class="form-label">メールアドレス <span class="required">*</span></label>
                                <input type="email" class="form-control" id="email" name="email" 
                                       value="<%= stall.email %>" required>
                            </div>
                            <div class="col-md-6">
                                <label for="phone" class="form-label">電話番号 <span class="required">*</span></label>
                                <input type="tel" class="form-control" id="phone" name="phone" 
                                       value="<%= stall.phone %>" required>
                            </div>
                        </div>

                        <div class="mb-3">
                            <label for="address" class="form-label">住所</label>
                            <input type="text" class="form-control" id="address" name="address" 
                                   value="<%= stall.address || '' %>">
                        </div>
                    </div>

                    <!-- 出店内容 -->
                    <div class="form-section">
                        <h5>🏪 出店内容</h5>
                        <div class="row mb-3">
                            <div class="col-md-6">
                                <label for="boothType" class="form-label">出店内容 <span class="required">*</span></label>
                                <select class="form-select" id="boothType" name="boothType" required>
                                    <option value="">選択してください</option>
                                    <option value="飲食" <%= stall.boothType === '飲食' ? 'selected' : '' %>>飲食</option>
                                    <option value="物販" <%= stall.boothType === '物販' ? 'selected' : '' %>>物販</option>
                                </select>
                            </div>
                            <div class="col-md-6">
                                <label for="boothCount" class="form-label">希望出店枠数</label>
                                <input type="number" class="form-control" id="boothCount" name="boothCount" min="1"
                                       value="<%= stall.boothCount || '' %>">
                            </div>
                        </div>

                        <div class="mb-3">
                            <label for="items" class="form-label">販売品目</label>
                            <textarea class="form-control" id="items" name="items" rows="3"><%= stall.items || '' %></textarea>
                        </div>

                        <div class="row mb-3">
                            <div class="col-md-6">
                                <label for="priceRangeMin" class="form-label">価格帯（最低価格）</label>
                                <input type="number" class="form-control" id="priceRangeMin" name="priceRangeMin" min="0"
                                       value="<%= stall.priceRangeMin || '' %>">
                            </div>
                            <div class="col-md-6">
                                <label for="priceRangeMax" class="form-label">価格帯（最高価格）</label>
                                <input type="number" class="form-control" id="priceRangeMax" name="priceRangeMax" min="0"
                                       value="<%= stall.priceRangeMax || '' %>">
                            </div>
                        </div>
                    </div>

                    <!-- 設営・レンタル -->
                    <div class="form-section">
                        <h5>⛺ 設営・レンタル情報</h5>
                        <div class="row mb-3">
                            <div class="col-md-4">
                                <label for="tentWidth" class="form-label">テント横幅（m）</label>
                                <input type="number" class="form-control" id="tentWidth" name="tentWidth" step="0.1" min="0"
                                       value="<%= stall.tentWidth || '' %>">
                            </div>
                            <div class="col-md-4">
                                <label for="tentDepth" class="form-label">テント奥行（m）</label>
                                <input type="number" class="form-control" id="tentDepth" name="tentDepth" step="0.1" min="0"
                                       value="<%= stall.tentDepth || '' %>">
                            </div>
                            <div class="col-md-4">
                                <label for="tentHeight" class="form-label">テント高さ（m）</label>
                                <input type="number" class="form-control" id="tentHeight" name="tentHeight" step="0.1" min="0"
                                       value="<%= stall.tentHeight || '' %>">
                            </div>
                        </div>

                        <div class="row mb-3">
                            <div class="col-md-6">
                                <label for="rentalTables" class="form-label">レンタルテーブル数</label>
                                <input type="number" class="form-control" id="rentalTables" name="rentalTables" min="0"
                                       value="<%= stall.rentalTables || '' %>">
                            </div>
                            <div class="col-md-6">
                                <label for="rentalChairs" class="form-label">レンタル椅子数</label>
                                <input type="number" class="form-control" id="rentalChairs" name="rentalChairs" min="0"
                                       value="<%= stall.rentalChairs || '' %>">
                            </div>
                        </div>
                    </div>

                    <!-- 搬入搬出 -->
                    <div class="form-section">
                        <h5>🚚 搬入搬出</h5>
                        <div class="row mb-3">
                            <div class="col-md-4">
                                <label for="vehicleCount" class="form-label">車両台数</label>
                                <input type="number" class="form-control" id="vehicleCount" name="vehicleCount" min="0"
                                       value="<%= stall.vehicleCount || '' %>">
                                <div class="form-text">（２台以上の場合は要相談）</div>
                            </div>
                            <div class="col-md-4">
                                <label for="vehicleType" class="form-label">車両種別</label>
                                <select class="form-select" id="vehicleType" name="vehicleType">
                                    <option value="">選択してください</option>
                                    <option value="キッチンカー" <%= (stall.vehicleType === 'キッチンカー') ? 'selected' : '' %>>キッチンカー（会場内駐車）</option>
                                    <option value="搬入・搬出車" <%= (stall.vehicleType === '搬入・搬出車') ? 'selected' : '' %>>搬入・搬出車（駐車スペースに駐車）</option>
                                </select>
                            </div>
                            <div class="col-md-4">
                                <label for="vehicleNumbers" class="form-label">車両ナンバー</label>
                                <input type="text" class="form-control" id="vehicleNumbers" name="vehicleNumbers"
                                       value="<%= stall.vehicleNumbers || '' %>" 
                                       placeholder="例: 横浜123あ4567">
                                <div class="form-text">複数台の場合はカンマ区切りで入力</div>
                            </div>
                        </div>
                        
                        <div class="alert alert-warning">
                            <strong>⚠️ 注意事項:</strong> 開催時間内は車両の移動はできません
                        </div>
                    </div>

                    <!-- 連絡事項 -->
                    <div class="form-section">
                        <h5>💬 連絡事項</h5>
                        <div class="mb-3">
                            <label for="questions" class="form-label">ご質問・ご要望等</label>
                            <textarea class="form-control" id="questions" name="questions" rows="4"><%= stall.questions || '' %></textarea>
                        </div>
                    </div>

                    <!-- 個人情報同意確認 -->
                    <div class="form-section">
                        <h5>🔒 個人情報の取り扱い同意</h5>
                        <div class="mb-3">
                            <div class="alert alert-light">
                                <div class="form-check mb-2">
                                    <span class="form-check-input" style="background-color: #d4edda; border-color: #28a745;">✓</span>
                                    <label class="form-check-label ms-2">
                                        ご入力いただいた個人情報を、本イベントの運営に必要な範囲で利用することに同意します。（必須）
                                    </label>
                                </div>
                                <div class="form-check">
                                    <span class="form-check-input" style="background-color: <%= stall.marketingConsent ? '#d4edda' : '#f8f9fa' %>; border-color: <%= stall.marketingConsent ? '#28a745' : '#6c757d' %>;">
                                        <%= stall.marketingConsent ? '✓' : '✗' %>
                                    </span>
                                    <label class="form-check-label ms-2">
                                        ご入力いただいた個人情報を、今後のイベントのお知らせに利用することに<%= stall.marketingConsent ? '同意します' : '同意しません' %>。（任意）
                                    </label>
                                </div>
                                <p class="small text-muted mt-2 mb-0">※個人情報同意は申込者本人による変更のみ可能です</p>
                            </div>
                        </div>
                    </div>

                    <!-- 変更理由・編集者 -->
                    <div class="form-section">
                        <h5>📋 変更情報</h5>
                        <div class="row mb-3">
                            <div class="col-md-6">
                                <label for="editor" class="form-label">編集者名 <span class="required">*</span></label>
                                <input type="text" class="form-control" id="editor" name="editor" required
                                       placeholder="例: 田中太郎">
                            </div>
                            <div class="col-md-6">
                                <label for="reason" class="form-label">変更理由 <span class="required">*</span></label>
                                <input type="text" class="form-control" id="reason" name="reason" required
                                       placeholder="例: 申込者からの変更依頼">
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="text-center mt-4">
                <button type="submit" class="btn btn-primary btn-lg me-3">💾 保存</button>
                <a href="/admin/stalls/<%= stall.id %>" class="btn btn-outline-secondary btn-lg">キャンセル</a>
            </div>
        </form>
    </main>

    <footer class="py-3 mt-4">
        <div class="container text-center text-muted">
            <p>&copy; 2025 菊名桜山公園 ボランティア管理システム</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>