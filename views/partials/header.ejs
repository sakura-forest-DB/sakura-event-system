<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= title || 'Forest Christmas 2025' %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; }
    .navbar-brand {
      font-weight: 700;
      color: #2d5a27 !important;
      font-size: 1.5rem;
    }
    .btn-volunteer {
      background: #2d5a27;
      border-color: #2d5a27;
      color: #fff;
      font-weight: 600;
    }
    .btn-volunteer:hover {
      background: #1e3c1a;
      border-color: #1e3c1a;
      color: #fff;
    }
    .btn-stall {
      background: #d63384;
      border-color: #d63384;
      color: #fff;
      font-weight: 600;
    }
    .btn-stall:hover {
      background: #b02a6a;
      border-color: #b02a6a;
      color: #fff;
    }
    .btn-performer {
      background: #6f42c1;
      border-color: #6f42c1;
      color: #fff;
      font-weight: 600;
    }
    .btn-performer:hover {
      background: #59359a;
      border-color: #59359a;
      color: #fff;
    }
    .btn-view-all {
      background: #0d6efd;
      border-color: #0d6efd;
      color: #fff;
      font-weight: 600;
    }
    .btn-view-all:hover {
      background: #0b5ed7;
      border-color: #0b5ed7;
      color: #fff;
    }
    .hero-section {
      background: linear-gradient(135deg, #2d5a27 0%, #4a8c3f 100%);
      color: white;
      padding: 3rem 0;
      border-radius: 10px;
      margin-bottom: 2rem;
    }
    .card {
      transition: transform 0.2s;
    }
    .card:hover {
      transform: translateY(-5px);
    }
/* ====== Christmas 2025 theme helpers ====== */
/* ※ 既存のトップのボタン色に合わせたい場合はこの2色を調整してください */
:root{
  /* 出店（濃いピンク系） */
  --stall-main: #e83e8c;     /* 例: Bootstrapのpink。今の濃いピンクに近ければOK */
  --stall-tint: #fde6f1;     /* 薄いピンク（背景用） */

  /* 出演（紫系） */
  --performer-main: #6f42c1; /* 例: Bootstrapのpurple。今の紫に近ければOK */
  --performer-tint: #efe7fb; /* 薄い紫（背景用） */

  /* 共通（ビュー“全体”の薄い背景を使うとき） */
  --hero-bg: #f5fbf6;        /* 既存トップの淡い緑に寄せたい場合はここを調整 */
}

/* メインCTA類（既存のクラス名に寄せて整え） */
.btn-stall {
  background: var(--stall-main);
  border-color: var(--stall-main);
  color:#fff;
}
.btn-stall:hover { filter: brightness(0.92); color:#fff; }

.btn-performer {
  background: var(--performer-main);
  border-color: var(--performer-main);
  color:#fff;
}
.btn-performer:hover { filter: brightness(0.92); color:#fff; }

.btn-view-all{
  background:#2d5a27;      /* 既存トップの緑ボタンに合わせる */
  border-color:#2d5a27;
  color:#fff;
}
.btn-view-all:hover { filter: brightness(0.92); color:#fff; }

/* ヒーローを中央寄せ（スマホ対応） */
.hero-center {
  text-align:center;
  padding: 2.5rem 0;
  background: var(--hero-bg);
  border-radius: .75rem;
}

/* フォームページ用の薄色背景（ページ全体に敷く） */
.form-tint--stall    { background: var(--stall-tint);    }
.form-tint--performer{ background: var(--performer-tint);}
.form-tint__wrap{
  /* 画面幅いっぱいの薄色 + 中央にカード */
  padding: 1.25rem 0;
}
.form-card{
  background:#fff;
  border-radius:.75rem;
  box-shadow:0 6px 18px rgba(0,0,0,.08);
  padding:1rem;
}

/* 上部のリンク群を“ボタン列”化（スマホ折返し） */
.top-links{
  gap:.5rem;
  flex-wrap: wrap;
}
  </style>
<link rel="stylesheet" href="/css/forms.css">  
</head>
<body class="d-flex flex-column min-vh-100">
  <nav class="navbar navbar-expand-lg navbar-light bg-light border-bottom">
    <div class="container">
      <a class="navbar-brand" href="/">🌸 菊名桜山公園</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav" aria-controls="mainNav" aria-expanded="false" aria-label="メニュー切替">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div id="mainNav" class="collapse navbar-collapse">
        <ul class="navbar-nav me-auto">
          <li class="nav-item"><a class="nav-link" href="/">ホーム</a></li>
          <li class="nav-item"><a class="nav-link" href="/register">ボランティア登録</a></li>
          <li class="nav-item"><a class="nav-link" href="/status">申込状況確認</a></li>
        </ul>
      </div>
    </div>
  </nav>
