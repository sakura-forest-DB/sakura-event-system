<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .navbar-brand { font-weight: bold; color: #2d5a27 !important; }
        .bg-primary { background-color: #2d5a27 !important; }
        .btn-primary { background-color: #2d5a27; border-color: #2d5a27; }
        .btn-primary:hover { background-color: #1e3c1a; border-color: #1e3c1a; }
        footer { background-color: #f8f9fa; margin-top: auto; }
    </style>
</head>
<body class="d-flex flex-column min-vh-100">
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container">
            <a class="navbar-brand" href="/">🌸 菊名桜山公園</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/">ホーム</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/register">ボランティア登録</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/apply">イベント申込</a>
                    </li>
                </ul>
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="/admin">管理画面</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <main class="container my-4 flex-grow-1">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-body text-center">
                        <div class="mb-4">
                            <div class="display-1">🎉</div>
                            <h2 class="text-success">申込完了</h2>
                        </div>
                        
                        <p class="lead">イベントへの申込が完了しました</p>
                        
                        <div class="alert alert-info text-start">
                            <h5>申込情報</h5>
                            <ul class="mb-0">
                                <li><strong>氏名:</strong> <%= volunteer.name %></li>
                                <li><strong>イベント:</strong> <%= event.title %></li>
                                <li><strong>日時:</strong> <%= new Date(event.date).toLocaleDateString('ja-JP') %></li>
                                <li><strong>場所:</strong> <%= event.location %></li>
                                <li><strong>役割:</strong> <%= signup.role %></li>
                                <li><strong>参加頻度:</strong> 
                                    <%= signup.freq === 'monthly' ? '月1回程度' : 
                                        signup.freq === 'weekly' ? '週1回程度' : '随時・不定期' %>
                                </li>
                                <% if (signup.details && Object.keys(signup.details).length > 0) { %>
                                    <li><strong>詳細:</strong>
                                        <% if (signup.details.tasks) { %>
                                            <%= Array.isArray(signup.details.tasks) ? signup.details.tasks.join(', ') : signup.details.tasks %>
                                        <% } %>
                                        <% if (signup.details.performanceType) { %>
                                            <%= signup.details.performanceType %>
                                        <% } %>
                                        <% if (signup.details.storeName) { %>
                                            <%= signup.details.storeName %>
                                        <% } %>
                                    </li>
                                <% } %>
                            </ul>
                        </div>
                        
                        <div class="alert alert-warning">
                            <strong>今後の流れ:</strong><br>
                            担当者より確認のご連絡をいたします。しばらくお待ちください。
                        </div>
                        
                        <div class="d-grid gap-2 d-md-flex justify-content-md-center">
                            <a href="/apply" class="btn btn-primary me-md-2">他のイベントにも申込む</a>
                            <a href="/" class="btn btn-outline-primary">ホームに戻る</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <footer class="py-3 mt-4">
        <div class="container text-center text-muted">
            <p>&copy; 2025 菊名桜山公園 ボランティア管理システム</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>