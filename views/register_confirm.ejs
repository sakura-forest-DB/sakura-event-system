<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .navbar-brand { font-weight: bold; color: #2d5a27 !important; }
        .bg-primary { background-color: #2d5a27 !important; }
        .btn-primary { background-color: #2d5a27; border-color: #2d5a27; }
        .btn-primary:hover { background-color: #1e3c1a; border-color: #1e3c1a; }
        footer { background-color: #f8f9fa; margin-top: auto; }
        .form-section { border-left: 4px solid #2d5a27; padding-left: 15px; margin-bottom: 2rem; }
        
        .volunteer-form-bg {
            background-color: #e8f8e8;
        }
        .card-header.volunteer-header {
            background-color: #c8e6c9;
            border-bottom: 1px solid #a5d6a7;
        }
        
        .form-label {
            background-color: #f8f9fa;
            border: 1px solid #dee2e6;
            padding: 4px 8px;
            border-radius: 3px;
            font-weight: bold;
            display: inline-block;
            margin-bottom: 6px;
            font-size: 0.9em;
        }
        
        .confirm-value {
            background-color: #fff;
            border: 1px solid #dee2e6;
            padding: 8px 12px;
            border-radius: 4px;
            min-height: 38px;
            display: flex;
            align-items: center;
        }
    </style>
</head>
<body class="d-flex flex-column min-vh-100">
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container">
            <a class="navbar-brand" href="/">🌸 菊名桜山公園</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/">ホーム</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/register">ボランティア登録</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/apply">イベント申込</a>
                    </li>
                </ul>
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="/admin">管理画面</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <main class="container my-4 flex-grow-1">
        <div class="row justify-content-center">
            <div class="col-md-10">
                <div class="card">
                    <div class="card-header volunteer-header">
                        <h4 class="mb-0">🌱 ボランティア登録内容確認</h4>
                        <small class="text-muted">内容をご確認の上、送信または戻って修正してください</small>
                    </div>
                    <div class="card-body volunteer-form-bg">
                        <!-- 基本情報 -->
                        <div class="form-section">
                            <h5>📝 基本情報</h5>
                            <div class="row mb-3">
                                <div class="col-md-6">
                                    <label class="form-label">登録種別</label>
                                    <div class="confirm-value">
                                        <span class="badge bg-<%= formData.type === 'org' ? 'info' : 'primary' %> fs-6">
                                            <%= formData.type === 'org' ? '団体' : '個人' %>
                                        </span>
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label"><%= formData.type === 'org' ? '団体名' : '氏名' %></label>
                                    <div class="confirm-value"><%= formData.name %></div>
                                </div>
                            </div>

                            <% if (formData.type === 'org' && formData.orgName) { %>
                            <div class="row mb-3">
                                <div class="col-md-6">
                                    <label class="form-label">団体名</label>
                                    <div class="confirm-value"><%= formData.orgName %></div>
                                </div>
                            </div>
                            <% } %>

                            <div class="row mb-3">
                                <div class="col-md-6">
                                    <label class="form-label">メールアドレス</label>
                                    <div class="confirm-value"><%= formData.email %></div>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label">電話番号</label>
                                    <div class="confirm-value"><%= formData.phone || '未入力' %></div>
                                </div>
                            </div>

                            <div class="mb-3">
                                <label class="form-label">住所</label>
                                <div class="confirm-value"><%= formData.address || '未入力' %></div>
                            </div>
                        </div>

                        <!-- スキル・経験 -->
                        <div class="form-section">
                            <h5>💡 スキル・経験</h5>
                            <div class="mb-3">
                                <label class="form-label">選択されたスキル</label>
                                <div class="confirm-value">
                                    <%
                                    const skillsArray = Array.isArray(formData.skills) ? formData.skills : (formData.skills ? [formData.skills] : []);
                                    %>
                                    <% if (skillsArray.length > 0) { %>
                                        <% skillsArray.forEach((skill, index) => { %>
                                            <span class="badge bg-secondary me-1"><%= skill %></span>
                                        <% }); %>
                                    <% } else { %>
                                        <span class="text-muted">選択なし</span>
                                    <% } %>
                                </div>
                            </div>
                        </div>

                        <!-- 興味のある活動 -->
                        <div class="form-section">
                            <h5>🌟 興味のある活動</h5>
                            <div class="mb-3">
                                <label class="form-label">一般活動</label>
                                <div class="confirm-value">
                                    <%
                                    const interestsArray = Array.isArray(formData.interests) ? formData.interests : (formData.interests ? [formData.interests] : []);
                                    %>
                                    <% if (interestsArray.length > 0) { %>
                                        <% interestsArray.forEach((interest, index) => { %>
                                            <span class="badge bg-info me-1"><%= interest %></span>
                                        <% }); %>
                                    <% } else { %>
                                        <span class="text-muted">選択なし</span>
                                    <% } %>
                                </div>
                            </div>

                            <div class="mb-3">
                                <label class="form-label">保全活動</label>
                                <div class="confirm-value">
                                    <%
                                    const conservationArray = Array.isArray(formData.conservationActivities) ? formData.conservationActivities : (formData.conservationActivities ? [formData.conservationActivities] : []);
                                    %>
                                    <% if (conservationArray.length > 0) { %>
                                        <% conservationArray.forEach((activity, index) => { %>
                                            <span class="badge bg-success me-1"><%= activity %></span>
                                        <% }); %>
                                    <% } else { %>
                                        <span class="text-muted">選択なし</span>
                                    <% } %>
                                </div>
                            </div>
                        </div>

                        <!-- 備考・メッセージ -->
                        <div class="form-section">
                            <h5>💬 備考・メッセージ</h5>
                            <div class="mb-3">
                                <label class="form-label">その他ご質問・ご要望等</label>
                                <div class="confirm-value" style="white-space: pre-wrap; min-height: 60px;">
                                    <%= formData.notes || '特になし' %>
                                </div>
                            </div>
                        </div>

                        <!-- 個人情報同意確認 -->
                        <div class="form-section">
                            <h5>🔒 個人情報の取り扱い同意</h5>
                            <div class="mb-3">
                                <div class="alert alert-light">
                                    <div class="form-check">
                                        <span class="form-check-input" style="background-color: #d4edda; border-color: #28a745;">✓</span>
                                        <label class="form-check-label ms-2">
                                            ご入力いただいた個人情報を、菊名桜山公園でのボランティア活動の運営や連絡、今後の活動のお知らせに必要な範囲で利用することに同意します。（必須）
                                        </label>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- 確認・送信ボタン -->
                        <div class="text-center mt-4">
                            <form method="POST" action="/register/submit" class="d-inline">
                                <% for (const [key, value] of Object.entries(formData)) { %>
                                    <% if (Array.isArray(value)) { %>
                                        <% value.forEach(v => { %>
                                            <input type="hidden" name="<%= key %>" value="<%= v || '' %>">
                                        <% }); %>
                                    <% } else { %>
                                        <input type="hidden" name="<%= key %>" value="<%= value || '' %>">
                                    <% } %>
                                <% } %>
                                <button type="submit" class="btn btn-success btn-lg me-3">✅ 内容確認後、登録する</button>
                            </form>
                            <form method="POST" action="/register/edit" class="d-inline">
                                <% for (const [key, value] of Object.entries(formData)) { %>
                                    <% if (Array.isArray(value)) { %>
                                        <% value.forEach(v => { %>
                                            <input type="hidden" name="<%= key %>" value="<%= v || '' %>">
                                        <% }); %>
                                    <% } else { %>
                                        <input type="hidden" name="<%= key %>" value="<%= value || '' %>">
                                    <% } %>
                                <% } %>
                                <button type="submit" class="btn btn-outline-secondary btn-lg">✏️ 戻って修正する</button>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <footer class="py-3 mt-4">
        <div class="container text-center text-muted">
            <p>&copy; 2025 菊名桜山公園 ボランティア管理システム</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>