<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body { background-color: #f7fdf8; }
    .navbar-brand { font-weight: bold; color: #2d5a27 !important; }
    .card-header { background-color: #2d5a27; color: #fff; }
    .btn-accent { background-color: #2d5a27; border-color: #2d5a27; color: #fff; }
    .btn-accent:hover { background-color: #244c22; border-color: #244c22; }
    .form-section { border-left: 4px solid #2d5a27; padding-left: 15px; margin-bottom: 2rem; }
    footer { background-color: #f8f9fa; margin-top: auto; }
  </style>
</head>
<body class="d-flex flex-column min-vh-100">
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
      <a class="navbar-brand" href="/">🌸 菊名桜山公園</a>
    </div>
  </nav>

  <main class="container my-4 flex-grow-1">
    <div class="row justify-content-center">
      <div class="col-md-10">
        <div class="card">
          <div class="card-header">
            <h4 class="mb-0">🎭 出演申込内容確認</h4>
            <small class="text-light">内容をご確認の上、送信または修正してください</small>
          </div>

          <div class="card-body">
            <!-- 基本情報 -->
            <div class="form-section">
              <h5>📝 基本情報</h5>
              <p><strong>参加団体名：</strong> <%= formData.groupName || '未入力' %></p>
              <p><strong>代表者名：</strong> <%= formData.representative || '未入力' %></p>
              <p><strong>メール：</strong> <%= formData.email || '未入力' %></p>
              <p><strong>電話番号：</strong> <%= formData.phone || '未入力' %></p>
              <p><strong>住所：</strong> <%= formData.address || '未入力' %></p>
            </div>

            <!-- 出演内容 -->
<div class="form-section">
  <h5>🎶 出演内容</h5>
  <p><strong>演目・内容：</strong> <%= formData.performance || '未入力' %></p>
  <p><strong>出演人数：</strong> <%= formData.performerCount || '未入力' %></p>
  <p><strong>希望出演枠数：</strong> <%= formData.slotCount || '未入力' %></p>
  <p><strong>音源のみ（カラオケ等）：</strong> <%= formData.audioSourceOnly ? 'あり' : 'なし' %></p>
  <p><strong>アンプ貸出希望：</strong> <%= formData.rentalAmp ? 'あり' : 'なし' %></p>
  <p><strong>マイク貸出希望：</strong> <%= formData.rentalMic || 'なし' %></p>
  <p><strong>車両台数：</strong> <%= formData.vehicleCount || '—' %></p>
  <p><strong>車両ナンバー：</strong> <%= formData.vehicleNumbers || '—' %></p>
</div>

            <!-- 個人情報同意 -->
            <div class="form-section">
              <h5>🔒 個人情報の取扱い同意</h5>
              <p>ご入力いただいた個人情報は、本イベント運営の目的でのみ利用します。</p>
            </div>

            <!-- 確認・送信ボタン -->
            <div class="d-grid gap-2 d-md-flex justify-content-md-end mt-4">
              <form method="GET" action="/apply/<%= event.slug %>/performer" class="me-md-2">
                <button type="submit" class="btn btn-secondary">✏️ 戻って修正</button>
              </form>

              <form method="POST" action="/apply/<%= event.slug %>/performer/submit">
                <button type="submit" class="btn btn-accent">✅ 内容確認後、送信する</button>
              </form>
            </div>

          </div>
        </div>
      </div>
    </div>
  </main>

  <footer class="py-3 mt-4">
    <div class="container text-center text-muted">
      <p>&copy; 2025 菊名桜山公園 ボランティア管理システム</p>
    </div>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>